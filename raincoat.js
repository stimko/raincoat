
var Raincoat = {

  baseURL: 'api.openweathermap.org/data/2.5/',

  getWeatherFor: function(arg){
    /* parse arguments
      Can be coordinates, city, id
    */
  },
  getWeeklyForecastFor: function(arg){
  	/* parse arguments
      Can be coordinates, city, id
    */
  },
  searchFor: function(arg){
     /* parse arguments
      Can be coordinates, city, accuracy
    */
  },
  inUnits: function(arg){
    //Metric or Imperial
  }
}